
<li><a href="{{ path('quizzes_list') }}">Quizzes</a></li>
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="display: inline-block; padding: 3px 0 3px 0">
        <img src="{{ asset('assets/img/user.png') }}" class="img-circle" style="height: 45px; margin-right: 5px">
        {% if app.user.fullname == "" %}
            {{ app.user.username }}
        {% else %}
            {{ app.user.fullname }}
        {% endif %}
        <b class="caret"></b></a>
    <ul class="dropdown-menu">
        <li><a href="{{ path('profile') }}">Profile</a></li>
        {% if role == 'admin' %}
            <li><a href="{{ path('admin_control') }}">Admin panel</a></li>
        {%  endif %}
        <li><a href="#">Settings</a></li>
        <li><a href="#">Quizzes of User</a></li>
        <li><a href="{{ path('logout') }}">Log out</a></li>
    </ul>
</li>